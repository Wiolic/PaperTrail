# 快速上手

写给完全没写过代码的人。你要亲手做的只有两件事——**装一个 AI 助手**、**下载这个仓库**——剩下的（装 Python、装依赖、配 API Key、建文件夹）全部交给 AI 帮你做完。整个过程大概十分钟。

---

## 第一步：装一个 AI 助手

AI 助手是这套系统的"操作员"——你跟它说话，它替你执行操作。四选一，不确定就选第一个：

**Claude Code**（推荐，本系统主要按它的使用习惯设计）
打开 [claude.ai/code](https://claude.ai/code)，按页面提示下载安装。装完后按 `Win+R` 输入 `powershell` 回车打开一个命令行窗口，输入 `claude` 回车，看到能打字聊天的界面就说明装好了。如果提示"不是内部或外部命令"，说明装的时候哪一步没完成，回官网重装一次。

**Qoder**
[qoder.ai](https://qoder.ai) 下载安装包装上，打开后选你解压出来的 PaperTrail 文件夹作为项目，它会自动识别配置，直接可以对话。也能当 VS Code/JetBrains 插件用。

**Codex CLI**
[openai.com/codex](https://openai.com/codex) 下载安装，PowerShell 里输入 `codex` 启动。需要另外配一下 DeepSeek 当后端模型，可以让 AI 装好之后帮你配，或参照 `prompts/codex-cli-guide.md`。

**Kimi Code**
搜"Kimi Code"官网下载安装，启动后配置用 DeepSeek 做模型。

这四个装哪个都行，之后随时可以换——所有数据都在你本地的文件夹里，跟你用哪个 AI 助手无关。

---

## 第二步：下载仓库

1. 打开这个项目的 GitHub 页面，点绿色的 **Code** 按钮 → **Download ZIP**
2. 解压到一个**路径不含中文和空格**的地方，比如 `D:\PaperTrail`
3. **不要**把这个文件夹放进 OneDrive/iCloud 之类的云同步文件夹

解压完你会看到 `scripts/`、`AGENTS.md`、`templates/` 这些文件——这就是你以后的文献库根目录。

---

## 第三步：让 AI 把剩下的都搞定

1. 打开 PowerShell，输入 `cd D:\PaperTrail`（换成你实际的解压路径）回车，进入这个文件夹
2. 启动你选的 AI 助手（Claude Code 输入 `claude`，Codex 输入 `codex`；Qoder 打开项目就自动进入了）
3. 把下面这段话复制进去，**把方括号里的内容换成你自己的研究方向**，发送：

```
你好，我刚下载了 PaperTrail 文献管理系统，我是零基础用户。
请帮我完成从零开始的完整环境搭建：

1. 检查我的电脑是否已安装 Python，如果没有，指导我安装
2. 安装运行所需的依赖库
3. 指导我去 DeepSeek 官网申请 API Key 并配置好
4. 创建所有必需的数据文件夹和初始文件
5. 跑一次体检确认环境正常
6. 告诉我接下来怎么使用

我的研究领域是 [比如：钙钛矿太阳能电池 / 肠道菌群与代谢 / 宏观经济政策]。
```

接下来 AI 会一步步带你走完剩下的设置，你基本不需要自己动手敲任何命令：

- 没装 Python 的话，AI 会指路到 [python.org](https://www.python.org/downloads/)，**唯一要注意的是安装界面底部勾上"Add python.exe to PATH"**再点安装
- 依赖库 AI 会自己装好，一两分钟
- DeepSeek API Key 需要你自己去 [platform.deepseek.com](https://platform.deepseek.com/) 用手机号注册申请（这一步涉及你的账号，AI 没法替你做）——申请出来是一串 `sk-` 开头的字符，**只显示一次，立刻复制粘贴给 AI**，充值 5~10 元够用很久；把 Key 给 AI 之后它帮你配置好环境变量
- 文件夹结构 AI 自动建好

配置好之后你的文件夹里会多出 `inbox/`（放新论文的地方）、`papers/`、`notes/` 等一堆自动管理的子目录——这些你都不用手动碰，跟 AI 说话就行。

---

## 之后怎么用

两种方式随便混着用。

### 方式一：直接跟 AI 说话

| 你说 | AI 做什么 |
|---|---|
| "入库" | 读 `inbox/` 里的 PDF，抽取信息、判断类型、打标签、查重、写笔记、归档 |
| "体检" | 检查库里各处数据是否一致，有没有重复收录的论文 |
| "查新 XX 领域" | 系统性搜近几年顶刊，找出你还没收录的新论文 |
| "引文献：[一段话]" | 把这段话拆句，逐句找该引用哪篇论文 |
| "在 Word 插入 [citekey] 引用" | 在光标处插入编号引用（需要 Windows + Word 已打开） |
| "写 XX 综述" | 按主题筛出相关笔记，起草一份对比综述初稿 |

举个例子——新收到 3 篇论文：把 PDF 拖进 `inbox/` 文件夹，跟 AI 说"入库"，等它读完告诉你处理结果，就完事了。

### 方式二：网页操作面板

不想打字的话，双击仓库根目录的 `PaperTrail Launcher.bat`，会自动在浏览器打开一个面板。如果双击没反应，打开 PowerShell，`cd` 到你的仓库路径，输入 `streamlit run scripts/ui/app.py` 回车。

面板里能做的事：

- **总览**：看库存统计，把 PDF 直接拖进网页就能触发入库
- **文献库**：搜索框同时支持标题/作者/关键词；也可以展开"语义检索"用一句模糊问题搜（比如"哪些论文提到了这个现象的降解机理"，不需要关键词命中）
- **体检**：点一下跑健康检查
- **引文献** / **扩充查新**：贴文字或填关键词，实时看进度，能随时停
- **Word**：插引用
- **命令**：不确定敲什么时，用大白话描述，AI 翻译成命令给你看，你确认了再点运行

关掉面板：找到启动时弹出的黑色命令行窗口关掉，或者在里面按 `Ctrl+C`。

---

## 常见问题

**AI 让我装 Python，具体怎么装？**
去 [python.org](https://www.python.org/downloads/) 下最新版，双击装，**装的时候界面底部勾上"Add python.exe to PATH"**再点 Install Now。装完打开 PowerShell 输入 `py --version`，看到版本号就是成功了。如果忘了勾这个选项：重新打开安装包选"Modify"，把这个勾补上，然后关掉所有 PowerShell 窗口重新打开。

**DeepSeek Key 怎么申请？会不会不安全？**
[platform.deepseek.com](https://platform.deepseek.com/) 手机号注册，"API Keys"页面创建。这串字符**只显示一次**，务必立刻保存好。它只存在你自己电脑上，不会被上传到别处。充值 5~10 元能处理几百篇论文的量。

**配好 Key 之后 AI 还是说找不到？**
配置环境变量之后要**关掉所有命令行窗口重新打开**才会生效，让 AI 重新检查一次。

**PDF 会被传到网上吗？**
不会，所有文件只留在你本地电脑上，AI 只是读取本地文件调用 API 处理文字，不上传 PDF 本身。

**中文关键词能搜/能查新吗？**
可以，会自动翻译成英文去检索。

**AI 写的笔记靠谱吗？**
生成的笔记会标成"skimmed"（略读），意思是没经过你本人核实。读不出来的信息 AI 会老实标"原文未提供"，不会瞎编。重要的论文建议自己过一遍确认之后把状态改成"read"。

**入库出错了/数据看起来乱了怎么办？**
跟 AI 说"体检"，它会检查一遍各处数据是否一致，列出问题。

**我已经在用 EndNote/Zotero，这个跟它们冲突吗？**
不冲突。EndNote/Zotero 继续管你的文献收集和 Word 引用格式，PaperTrail 管精读笔记和智能检索。PaperTrail 可以把筛好的文献导出成 RIS/BibTeX 供它们导入。

**遇到别的问题？**
把报错信息直接复制粘贴给 AI，说"帮我看看这个问题"，大多数时候它能自己诊断修复。还是解决不了的话，把完整的错误信息截图，找人帮忙时贴出来比口头描述有效得多。

---

## 想深入了解？

- [`README.md`](README.md)——系统整体介绍
- [`AGENTS.md`](AGENTS.md)——数据格式规范，AI 自动读取，一般你不用看
- [`CLAUDE.md`](CLAUDE.md)——给 Claude Code 这类工具的角色说明
- [`ARCHITECTURE.md`](ARCHITECTURE.md)——技术实现细节，想改代码/接前端时看
