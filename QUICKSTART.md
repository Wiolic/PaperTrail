# 新手使用指南（零编程基础也能跑起来）

这份文档假设你**没写过代码、没用过命令行**，只是想把自己攒的一堆论文 PDF 整理成一个能搜索、能问问题、能自动写综述的"文献库"。跟着下面的步骤一步步做，预计 30~60 分钟能跑起来第一次入库，处理论文的 API 花费也很低（实测入库 400 篇论文总共约 10 元人民币，具体见下方"常见问题"）。

如果你懂技术、想直接看规范和脚本细节，看 [README.md](README.md)（整体设计）和 [AGENTS.md](AGENTS.md)（数据格式规范）。这份文档只讲"怎么照着做"，不讲"为什么这么设计"。**用 Codex CLI 的另外看一眼 [prompts/codex-cli-guide.md](prompts/codex-cli-guide.md)**，里面有配置模型 provider 的具体步骤。

---

## 第一步：你需要准备什么

1. **一台电脑**（Windows/Mac 都行）。
2. **一个能帮你操作电脑的 Agent CLI**，三选一都行，用法这份文档里完全一致：
   - **Claude Code**：到 [claude.com/claude-code](https://claude.com/claude-code) 按说明安装。
   - **Codex CLI**（OpenAI 出的同类工具）：到 [OpenAI 的 Codex CLI 文档](https://github.com/openai/codex)按说明安装。
   - **Kimi Code**（Moonshot 出的同类工具）：到 Moonshot 官方文档按说明安装。**用 Claude Code/Codex CLI 使用受限的（比如没有海外支付方式、访问不稳定）可以优先考虑这个**，国内可直接注册使用，配置和使用方式跟着这份指南走完全一样。
   
   装好之后，你会得到一个可以在里面打字聊天的"终端"窗口——不用怕，你要做的事就是在里面用**中文**打字告诉它你想干什么，跟聊天软件一样。这份指南后面统一写"你的 Agent CLI"，不管你装的是哪一个，把对应的话贴给它就行。
   > **差别只有一处**：Claude Code 打开这个文件夹会自动读 `CLAUDE.md` 里的操作规则；Codex CLI / Kimi Code 只会自动读 `AGENTS.md`（数据格式规范），不会自动读 `CLAUDE.md`（里面是给"架构维护者"角色用的体检/查重/综述这类进阶操作说明）。如果你用 Codex 或 Kimi Code，第一次对话时多说一句：**"也读一下 CLAUDE.md，按里面的角色说明来"**，之后它就知道这些操作了。
3. **一个 LLM API Key**（这是这套系统用来"读论文、写笔记"的 AI 服务，不需要用最贵的模型，便宜的模型就够用）。推荐 [DeepSeek](https://platform.deepseek.com)（性价比高，几块钱能处理几十篇论文），也可以换成 Kimi、通义千问、GLM、OpenAI 官方 API，或者你自己本地跑的模型——只要是"OpenAI 兼容"接口都能用（换的话让 Agent CLI 帮你改 `scripts/ds.py` 里的 `base_url` 和读取的环境变量名，一句话的事）。注册账号，在后台生成一个 API Key（一长串以 `sk-` 开头的字符），复制下来，账户里充值一点钱。
4. 一堆你想整理的**论文 PDF**。

你不需要会写代码——所有实际操作都是"你说一句话，Agent CLI 帮你跑脚本"。

---

## 第二步：把这套系统装到你的电脑上

打开你的 Agent CLI 的终端窗口，找到你想放这个文献库的位置（比如 `D:\我的文献库`），然后照着输入：

```bash
git clone <这个仓库的地址> 我的文献库
cd 我的文献库
pip install -r requirements.txt
```

如果你不知道什么是 `git clone`——直接把这段话贴给它：
> "帮我把 [仓库地址] clone 到 D:\我的文献库，然后装好 requirements.txt 里的依赖，然后读一下 AGENTS.md 和 CLAUDE.md 了解一下这是个什么系统"

它会自己执行（**用 Codex CLI 或 Kimi Code 的话记得加上"读一下 CLAUDE.md"这句**——它们默认只自动读 AGENTS.md，见上一节的差别说明）。

### 配置 API Key

把第一步拿到的 API Key 告诉它（下面以 DeepSeek 为例，换成别的 API 就把名字换掉）：
> "帮我把环境变量 DEEPSEEK_API_KEY 设置成 sk-xxxxxxxx（我刚才注册的那个key）"

Windows 上这一步会用 `setx` 写进系统环境变量，写完之后**新开一个终端窗口**才会生效（如果当前窗口已经在用，让它用 `$env:DEEPSEEK_API_KEY="sk-xxx"` 补一次当前会话）。

---

## 第三步：告诉它这是"什么领域"的文献库

这一步很重要，决定了以后它整理笔记、判断"这篇论文和我的课题有没有关系"时准不准。

打开 `AGENTS.md`，搜索里面标了 **`[领域定制]`** 的地方（用文本编辑器的搜索功能，或者直接让 Agent CLI 帮你找："AGENTS.md 里哪些地方标了[领域定制]，列给我看"），把示例内容换成你自己的情况，主要是这几处：

- 你的研究方向/课题背景是什么（这段话会被塞进每次 AI 整理论文的提示词里，帮它判断"这篇论文和我有多相关"）
- 你希望笔记里记录哪些"专属字段"（比如材料学可能想记"表征方法"，做算法的可能想记"数据集/评价指标"）
- 你常用的标签词表（比如你的领域里常见的几个技术关键词）
- 你心目中的"顶刊"名单（在 `scripts/top_journals.txt` 里改）

不知道怎么写也没关系，直接跟 Agent CLI 说：
> "我是做 XX 研究的，主要关注 XX 方向，帮我把 AGENTS.md 和 templates/note-template.md 里标 [领域定制] 的地方按我这个方向改一下"

它会帮你改，改完可以再检查一遍。这一步花点时间做扎实，后面用起来会顺很多；随便应付一下也不影响系统能跑，以后随时可以再改。

---

## 第四步：整理你的第一批论文

1. 把 PDF 文件（可以是几篇也可以是几十篇）拷贝到 `inbox` 这个文件夹里（如果没有这个文件夹，跟 Agent CLI 说一声，它会建）。
2. 在 Agent CLI 里就打两个字：
   > **入库**
3. 它会自动：读每篇 PDF → 调用你配置的 API 抽取标题/作者/摘要/方法/关键结果这些信息 → 检查你库里是不是已经有这篇了（避免重复）→ 生成一份结构化笔记 → 把 PDF 归档、更新索引表。处理完会告诉你："新增了哪几篇、跳过了哪几篇重复的、还有哪几篇它拿不准需要你确认"。
4. 处理完之后，`inbox` 文件夹会被清空（成功入库的和确认重复的都会被自动删除原文件，只有它吃不准的会留着）。

一次丢 10~20 篇问题不大；几十篇以上建议先跟它说一声"这批大概要处理XX篇，先给我估个数"，因为这会花你 API 账户里的一点额度。

---

## 第五步：日常怎么用

整理好之后，直接用自然语言问它就行，不用记任何命令：

| 你想做什么 | 直接跟它说 |
|---|---|
| 把新论文整理进库 | "入库" |
| 检查库里有没有问题（重复/缺笔记/格式不对） | "体检" |
| 想知道某个主题目前研究进展 | "帮我整理一份关于 XX 主题的综述" |
| 写论文时想确认某句话能引用哪些文献 | 把那句话贴给它，说"引文献" |
| 想把推荐的引用导入 EndNote/Zotero | "把这些引用导出成 EndNote 能导入的格式" |
| 没装 EndNote，想直接在 Word 光标处插入引用 | 在 Word 里把光标停在你要插入引用的地方，跟 Agent CLI 说"帮我在光标处插入 XX 这篇引用"（仅 Windows，文档要已经在 Word 里打开着） |
| 删了/挪动了引用，编号乱了想重新排 | 说"帮我重新给这篇文档的引用编号"（对应 `--rebuild`） |
| 懒得一句句挑，想让它自动扫全文找该引用的地方 | 在 Word 里打开你的文档，说"帮我扫一遍这篇文档，看看哪里需要加引用"——它会先给你看一遍预览，确认没问题再说"可以，插入吧" |
| 想搜某个方向近几年顶刊有什么新文章 | "扩充 XX 方向的文献"（第一次，全量搜近几年）或 "查新 XX 方向"（之后，只搜新增的） |
| 想把某个主题相关的 PDF 单独打包给别人 | "把 XX 主题的论文导出到一个文件夹" |

它每次操作完都会给你一份汇总（做了什么、新增了什么、哪些地方需要你自己确认），重要的删除/覆盖操作它不会自作主张，会先跟你说。

---

## 常见问题

**Q: 处理论文要花钱吗？大概多少钱？**
A: 花的是你 API 账户里的余额，成本很低。粗算一下：一篇论文全文大概是 8,000~20,000 token 的输入，生成的结构化笔记大概是 1,500~3,000 token 的输出，用 DeepSeek 这类便宜档位的模型，**单篇论文的成本大概是几分钱人民币、甚至更低的量级**。有实测参考：**入库 400 篇论文，总共只花了约 10 元人民币**（平均下来单篇不到 3 分钱）。写论文时用的"引文献"逐句添加引文、"扩充/查新"搜索候选抽取这些功能，单次调用的量级比整篇入库还小，成本可以忽略不计。这套系统默认用便宜模型干体力活（抽取、格式化），费钱的深度判断（查重、核实、综述归纳）留给你手上的 Agent CLI（通常按你自己的 Claude/Codex 订阅或额度算，不额外多花钱），这也是整体成本压得低的原因。想精确算账，看 [README.md](README.md) 里的成本核算公式，换成你实际用的 API 定价套进去就行。

**Q: 我的论文 PDF 会被上传到什么地方吗？**
A: PDF 本身不会上传，只有从 PDF 里提取出的文字会发送给你配置的那个 LLM API 用来生成结构化信息，这和你直接把论文内容贴给一个 AI 聊天软件本质上是一回事。

**Q: 我不小心把这套系统的代码传到了公开的 GitHub 仓库，会不会泄露我的论文？**
A: 不会——这套系统设计上就是"骨架和数据分开"：你实际的论文 PDF、笔记、索引这些都不在这个骨架仓库里（看 `.gitignore` 就知道），除非你自己手动把 `papers/`、`notes/` 这些文件夹也传上去。

**Q: 出错了怎么办？**
A: 把 Agent CLI 报的错误信息整个复制给它，说"这是什么问题，帮我看看"，它一般能自己诊断和修复。

**Q: 我想加新功能/改现有规则，怎么办？**
A: 直接跟 Agent CLI 描述你想要的行为，比如"我希望入库的时候还要检查一下XX"，它会帮你改脚本或者改 `AGENTS.md`/`CLAUDE.md` 里的规则说明。这套系统本来就是设计成可以持续跟 AI 一起迭代的，不需要你自己写代码。

**Q: 我用 Codex CLI 或 Kimi Code，是不是有什么地方要注意？**
A: 唯一要注意的：这两个默认打开文件夹只会自动读 `AGENTS.md`（数据格式规范），不会像 Claude Code 那样自动读 `CLAUDE.md`（架构维护者角色说明，包含"体检"/"入库"/"引文献"这些操作指令）。第一次对话时明确让它也读一下 `CLAUDE.md` 就行，之后它就有了同样的操作能力，其余流程和这份指南里写的完全一样。
