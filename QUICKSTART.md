# PaperTrail 快速上手指南

> **面向从未写过代码的用户**。你只需要手动完成两件事——**装 Claude Code** 和 **下载仓库**，剩下的全部交给 AI 帮你搞定（包括装 Python、装依赖、配 API Key）。
>
> 全程大约 10 分钟。

---

## 目录

- [你会得到什么](#你会得到什么)
- [Step 1：安装 AI 助手](#step-1安装-ai-助手)
- [Step 2：下载仓库](#step-2下载仓库)
- [Step 3：让 AI 帮你搞定一切](#step-3让-ai-帮你搞定一切)
- [开始日常使用](#开始日常使用)
- [网页操作面板](#网页操作面板)
- [日常科研流程](#日常科研流程)
- [常见问题 FAQ](#常见问题-faq)
- [遇到问题怎么办](#遇到问题怎么办)

---

## 你会得到什么

PaperTrail 是一套**跟 AI 对话就能用**的个人文献知识库系统。它能帮你：

1. **自动入库**：把论文 PDF 拖进来，AI 自动读取、生成精读笔记、查重、归档
2. **智能检索**：用大白话搜文献（"哪些论文用了 operando XAS"）
3. **系统性查新**：自动搜近 5 年顶刊，发现你可能漏掉的论文
4. **写论文找引文**：贴一段正文，AI 逐句帮你找该引哪篇
5. **Word 插引用**：在 Word 里一键插入编号引用
6. **生成综述**：按主题筛选笔记，生成对比综述初稿

全程**不需要你写代码**，用大白话跟 AI 说话就行。

---

## Step 1：安装 AI 助手

AI 助手是本系统的"大脑"——你跟它对话，它帮你完成所有操作（包括安装环境、配置依赖、管理文献）。

**请从以下四种中任选一种安装。** 推荐 Claude Code，因为本系统主要基于 Claude 生态构建，体验最好。

### 选项 1：Claude Code（推荐）

1. 打开浏览器，访问 **[https://claude.ai/code](https://claude.ai/code)**
2. 页面上会有下载/安装的按钮或指引，按提示操作即可（通常就是下载一个安装包，双击运行）
3. 安装完成后，按 `Win + R`，输入 `powershell`，按回车，会打开一个黑色窗口（叫"PowerShell"，可以理解为你跟电脑对话的另一个窗口）
4. 在这个黑色窗口里输入 `claude` 然后按回车
5. 如果看到了 Claude Code 的对话界面（可以打字跟它聊天的界面），说明安装成功了

> **什么是"终端/PowerShell"？** 就是 Windows 上一个可以输入文字命令的黑色窗口。打开方式：按键盘 `Win + R`（Win 键就是键盘左下角有 Windows 图标的那个键），在弹出的"运行"框里输入 `powershell`，点确定。后面所有需要用到它的地方，都是这样打开。

> **如果输入 claude 后提示"不是内部或外部命令"**：说明安装没有完全成功。回到 [https://claude.ai/code](https://claude.ai/code) 页面重新按提示操作一遍。如果还是不行，先试试备选方案。

### 选项 2：Qoder

1. 打开浏览器，访问 **[https://qoder.ai](https://qoder.ai)**
2. 页面上会有下载按钮，点击下载安装包
3. 下载完成后双击安装包运行，按提示一路安装即可
4. 安装完成后打开 Qoder，它会让你选择项目文件夹——选你下载的 PaperTrail 仓库文件夹
5. Qoder 会自动识别项目配置，直接可以开始对话

> Qoder 也可以作为 VS Code / JetBrains 编辑器的插件使用。如果你已经在用这些编辑器，可以在编辑器的插件市场搜索 "Qoder" 安装。

### 选项 3：Codex CLI

1. 打开浏览器，访问 **[https://openai.com/codex](https://openai.com/codex)**
2. 按页面指引下载安装包
3. 下载完成后双击安装包运行，按提示一路安装即可
4. 安装完成后打开 PowerShell（按 `Win + R` → 输入 `powershell` → 回车），输入 `codex` 按回车
5. 如果看到 Codex 的对话界面，说明安装成功了

> Codex CLI 需要配置 DeepSeek 作为后端模型。启动后可以让 AI 帮你配置，或参考 [`prompts/codex-cli-guide.md`](prompts/codex-cli-guide.md)。

### 选项 4：Kimi Code

1. 打开浏览器，访问 **Kimi Code 官网**（搜索"Kimi Code"即可找到）
2. 下载对应的安装包，双击运行，按提示安装
3. 安装完成后按指引启动，配置使用 DeepSeek 作为模型

> **选择建议**：如果你不确定选哪个，就选 **Claude Code**。这四种可以随时切换，不影响你的数据——所有笔记和文件都存在本地，跟 AI 助手无关。

---

## Step 2：下载仓库

"仓库"就是 PaperTrail 的全部代码文件。你需要把它下载到电脑上。

1. 打开本项目的 GitHub 页面
2. 找到页面中上方的绿色按钮 **"Code"**，点它
3. 在弹出的菜单里点 **"Download ZIP"**
4. 等下载完成后，找到下载的 ZIP 文件，**右键点击 → 选"全部解压缩"**
5. 选一个解压位置，比如 `D:\PaperTrail`
   - **注意：路径里不要有中文和空格**，推荐纯英文，比如 `D:\PaperTrail` 或 `C:\Projects\PaperTrail`

解压后你会得到一个文件夹，里面有 `scripts/`、`templates/`、`AGENTS.md` 等文件。

**这个文件夹就是你的文献库，以后所有操作都在这里进行。请不要把它放在 OneDrive/iCloud 等云同步文件夹里。**

---

## Step 3：让 AI 帮你搞定一切

这是最关键的一步。你要在 AI 助手里发一段"开场白"，之后 **AI 会自动帮你安装 Python、装依赖库、申请和配置 API Key、创建目录结构**——全程你只需要按 AI 的提示回答几个问题、点几个按钮。

### 操作步骤

1. 打开 PowerShell（按 `Win + R` → 输入 `powershell` → 回车）

2. 进入你解压的文件夹。在黑色窗口里输入下面的命令然后按回车（把 `D:\PaperTrail` 换成你实际解压的位置）：
   ```
   cd D:\PaperTrail
   ```
   > `cd` 是一个命令，意思是"进入这个文件夹"。就像你在文件管理器里双击打开文件夹一样。

3. 启动 AI 助手（根据你在 Step 1 选的）：
   - Claude Code：输入 `claude` 然后按回车
   - Qoder：在 VS Code 里打开 Qoder 对话面板（不需要这一步，Qoder 已经知道你打开了哪个文件夹）
   - Codex CLI：输入 `codex` 然后按回车

4. AI 助手启动后，**把下面这段话复制、粘贴到对话框里**，然后把 `[填写你的领域]` 换成你自己的研究方向，按回车发送：

```
你好，我刚下载了 PaperTrail 文献管理系统，我是零基础用户。
请帮我完成从零开始的完整环境搭建：

1. 检查我的电脑是否已安装 Python，如果没有，指导我安装
2. 安装运行所需的依赖库
3. 指导我去 DeepSeek 官网申请 API Key 并配置好
4. 创建所有必需的数据文件夹和初始文件
5. 跑一次体检确认环境正常
6. 告诉我接下来怎么使用

我的研究领域是 [填写你的领域，比如：酸性电解水OER催化剂/钙钛矿太阳能电池/锂离子电池正极材料]。
```

### 接下来会发生什么

AI 会一步步引导你完成所有设置。以下是大致的流程，你**不需要自己执行任何命令**——AI 会替你操作，你只需要按它的提示做：

| AI 做的事 | 你可能需要做的 |
|----------|---------------|
| 检查有没有装 Python | 如果没有，AI 会告诉你去 [python.org](https://www.python.org/downloads/) 下载安装。**安装时唯一要注意的是：在安装界面底部勾选 "Add python.exe to PATH"**，然后点 Install Now |
| 安装依赖库（系统运行需要的工具包） | 不需要做任何事，AI 会自己安装。大约需要 1~3 分钟 |
| 指导你申请 DeepSeek API Key | AI 会让你打开 [platform.deepseek.com](https://platform.deepseek.com/)，用手机号注册，创建一个 Key。Key 是一串以 `sk-` 开头的字符，**它只显示一次，务必立刻复制保存**。把 Key 告诉 AI，它帮你配好。充值 5~10 元就够用很久 |
| 创建文件夹和初始文件 | 不需要做任何事，AI 自动创建 |
| 运行体检 | 看 AI 给出的报告，确认一切正常 |

**完成后，你的文件夹里会多出这些内容：**

```
PaperTrail/
├── inbox/              ← 以后把新论文 PDF 扔到这里
├── papers/             ← AI 归档后的 PDF（自动管理，不用手动碰）
├── notes/              ← AI 生成的精读笔记（自动管理）
├── notes-readable/     ← 给人阅读的笔记版本（自动管理）
├── extracted-text/     ← PDF 文字缓存（自动管理）
├── topics/             ← 主题综述（自动管理）
├── exports/            ← 导出的文档（自动管理）
├── data/               ← 结构化数据（自动管理）
├── embeddings/         ← 语义搜索索引（自动管理）
├── library.bib         ← 引用库文件
├── INDEX.md            ← 总索引表
└── ...
```

**初始化完成后，系统就可以用了！**

---

## 开始日常使用

有两种使用方式，可以混合使用：

### 方式 A：跟 AI 说话（核心方式）

在 AI 助手的对话窗口里，直接用自然语言下指令：

| 你说的话 | AI 做什么 | 使用场景 |
|---------|-----------|---------|
| **"入库"** | 读取 inbox/ 里的 PDF → 抽元数据 → 查重 → 写笔记 → 归档 | 拿到新论文时 |
| **"体检"** | 检查各文件是否一致，找重复论文 | 定期维护（建议每周一次） |
| **"扩充 XX 领域 top journals"** | 系统性搜近 5 年顶刊，去重后导出候选清单 | 找新文献 |
| **"查新 XX 领域"** | 只搜上次扫描以来的新文章 | 定期追踪新论文 |
| **"引文献：[一段话]"** | 把这段话拆成句子，逐句找该引哪篇 | 写论文时 |
| **"在 Word 插入 [citekey] 引用"** | 在 Word 光标处插入编号引用 | 写论文时（仅 Windows） |
| **"写 XX 综述"** | 按主题筛选笔记，生成对比综述初稿 | 写综述时 |

**举个例子**——入库 3 篇新论文：

1. 打开文件管理器，把 3 个 PDF 文件复制/拖拽到 `inbox/` 文件夹
2. 在 AI 助手的对话窗口里输入：**"入库"**，按回车
3. AI 自动完成所有工作：读 PDF → 提取信息 → 查重 → 写笔记 → 归档
4. AI 告诉你："成功入库 3 篇，跳过了 0 篇重复"

### 方式 B：网页操作面板

如果你不想跟 AI 说话，也可以用一个可视化的网页面板来操作。

**Windows 用户**：打开文件管理器，进入你的仓库文件夹，**双击 `PaperTrail Launcher.bat`**。会自动在浏览器里打开面板。

**如果双击没反应**：打开 PowerShell，先输入 `cd D:\PaperTrail`（换成你的路径）按回车，再输入 `streamlit run scripts/ui/app.py` 按回车。

浏览器会自动打开一个页面，面板有 7 个标签页可以点：

| 标签页 | 功能 | 说明 |
|--------|------|------|
| **📊 总览** | 库存统计、拖拽 PDF 入库 | 首页，一眼看到库有多少篇 |
| **📚 文献库** | 搜索/筛选/浏览笔记 | 日常查文献的主界面 |
| **🩺 体检** | 对账 + 查重 | 一键检查库的健康状态 |
| **📝 引文献** | 贴正文 → 找引文 | 写论文时用 |
| **🔎 扩充/查新** | 系统性检索 | 发现领域新论文 |
| **📄 Word** | Word 引用管理 | 在 Word 里插引用（Windows） |
| **⚙️ 命令** | 自然语言→命令 | 不确定说什么时来这里翻译 |

> **关闭面板**：找到启动面板时弹出的那个黑色窗口，直接关掉它。或在里面按 `Ctrl + C`。

---

## 网页操作面板

上面已经介绍了如何启动面板。这里补充几个使用技巧：

- **拖拽入库**：在"总览"页面，直接用鼠标把 PDF 文件拖进页面就能入库
- **搜索**：在"文献库"页面，支持按关键词、标签、作者、年份等多维搜索
- **语义搜索**：在"文献库"页面切换到"语义搜索"模式，可以用模糊问题搜（如"哪些论文研究了 Ir 溶解机理"）
- **导出**：综述/引文结果可以导出为 Word（.docx）或 Excel（.xlsx）

---

## 日常科研流程

```
  发现新论文
  └→ 把 PDF 拖进 inbox/ 文件夹
     └→ 对 AI 说 "入库"
        └→ AI 自动：读 PDF → 抽信息 → 查重 → 写笔记 → 归档

  想找某篇论文的信息
  └→ 打开网页面板搜索 / 问 AI "XX 论文讲了什么"

  想知道领域有没有新论文
  └→ 对 AI 说 "查新 XX 领域"

  写论文时需要引用
  └→ 对 AI 说 "引文献：[你的正文段落]"
     └→ AI 逐句告诉你该引哪篇
        └→ 对 AI 说 "在 Word 插入 xxx 引用"

  写综述
  └→ 对 AI 说 "写 XX 综述"
     └→ AI 筛选笔记 → 生成对比初稿 → 导出 Word

  定期维护
  └→ 对 AI 说 "体检"
```

---

## 常见问题 FAQ

### 安装相关

**Q：AI 让我装 Python，该怎么装？**
去 [python.org](https://www.python.org/downloads/) 下载最新版 Python。下载完双击安装，**安装界面底部有一个勾选框 "Add python.exe to PATH"，一定要勾上**，然后点 "Install Now"。装完后打开 PowerShell 输入 `py --version`，看到 `Python 3.x.x` 就是成功了。如果忘了勾 PATH：重新双击安装包 → 选 "Modify" → 一路下一步到最后 → 勾上 "Add Python to environment variables" → 关掉所有 PowerShell 窗口重新打开再试。

**Q：DeepSeek API Key 怎么申请？安全吗？**
去 [platform.deepseek.com](https://platform.deepseek.com/) 用手机号注册，登录后在 "API Keys" 页面点"创建 API Key"。会生成一串以 `sk-` 开头的字符——**这个 Key 只显示一次，务必立刻复制保存**（忘了就删了重新创建）。在 "费用" 页面充值 5~10 元就够处理几百篇论文了。Key 只存在你本机电脑上，不会上传到任何地方。

**Q：API Key 配好了但 AI 说找不到？**
配置 Key 后需要**关掉所有 PowerShell 窗口重新打开**才能生效。重新打开后让 AI 再检查一次。

### 使用相关

**Q：入库后论文 PDF 会被上传到哪里吗？**
不会。所有文件只存在你本地电脑上。

**Q：搜索支持中文吗？**
支持。扩充/查新功能完全支持中文关键词，系统会自动翻译成英文搜索。日常搜索中英文都行。

**Q：AI 入库的笔记准确吗？**
AI 生成的笔记会标记为 `skimmed`（略读），表示未经人工精读验证。AI 读不到的信息会如实标注"原文未提供"，不会瞎编。重要论文建议入库后自己过一遍，确认后状态改为 `read`。

**Q：入库出错了怎么办？**
对 AI 说"体检"，AI 会自动检查各文件是否一致，找出问题并修复。

**Q：想换 AI 助手怎么办？**
直接换就行，数据完全不受影响。所有笔记和文件都存在本地，跟 AI 助手无关。

**Q：我有 EndNote，PaperTrail 能跟它配合吗？**
可以。EndNote 负责文献收集和 Word 引文管理，PaperTrail 负责精读笔记和智能检索。两者各司其职。PaperTrail 可以导出 RIS/BibTeX 供 EndNote 导入。

---

## 遇到问题怎么办

按优先级尝试：

1. **直接把报错信息复制给 AI**，说"帮我排查这个问题"——大多数问题 AI 会自动诊断和修复
2. **对 AI 说"体检"**：让 AI 跑一遍全库健康检查
3. **关掉所有黑色窗口（PowerShell）重新打开**：很多设置需要新窗口才生效
4. **把完整的错误信息截图发给帮你的人**：如果你要找别人帮忙，截图比口述有效得多

---

## 进一步了解

| 文件 | 内容 | 适合谁 |
|------|------|--------|
| [`AGENTS.md`](AGENTS.md) | 数据格式与内容生产规范 | AI 助手自动读取，一般不需要你手动看 |
| [`CLAUDE.md`](CLAUDE.md) | 架构维护者角色说明 | 想了解系统如何维护时看 |
| [`ARCHITECTURE.md`](ARCHITECTURE.md) | 系统架构和脚本清单 | 想了解技术细节/想二次开发时看 |
| [`prompts/codex-cli-guide.md`](prompts/codex-cli-guide.md) | Codex CLI 使用指南 | 使用 Codex CLI 作为 AI 助手时看 |
| [`README.md`](README.md) | 项目总览（含输入输出示例） | 快速了解系统功能 |
