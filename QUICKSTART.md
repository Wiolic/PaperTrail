# QUICKSTART —— 下载后，直接和 AI 对话就能用起来

这套系统是**对话驱动**的：你不需要会写代码、不需要懂 git、不需要背命令行参数。你要做的就是——

1. 装一个 **AI 编程助手（Agent CLI）**，
2. 把这个文件夹下载下来、用助手打开它，
3. **粘贴一段开场白，让 AI 自己帮你把环境配好**，
4. 然后就用大白话跟它说"入库""体检""帮我找这句话能引哪些文献"就行了。

全程大概 20~40 分钟，其中大部分时间是等下载和安装。下面每一步都给了可以直接照做/复制的内容。

---

## 你需要准备两样东西

| 东西 | 是什么 | 怎么来 |
|---|---|---|
| **一个 Agent CLI** | 能读你本地文件、能帮你跑命令的 AI 助手 | 三选一，见 Step 1 |
| **一个大模型 API Key** | 让系统能调用 AI 去读 PDF、抽取信息 | 见 Step 3（DeepSeek 最便宜，几块钱能处理几百篇） |

> **为什么需要两个 AI？** Agent CLI 是"总管"，负责跟你对话、做判断（查重、核实、综述）；API Key 背后的模型是"流水线工人"，负责把读 PDF、抽元数据这种批量体力活便宜地跑完。总管贵、工人便宜，分开用最省钱。

---

## Step 1 · 装一个 Agent CLI（三选一）

任选其一，装好任意一个就行：

- **Claude Code**（推荐，本系统原生支持）：Anthropic 官方 CLI，装法见 <https://claude.com/claude-code>。
- **Codex CLI**：OpenAI 的开源 CLI。用它的话额外看一眼 [prompts/codex-cli-guide.md](prompts/codex-cli-guide.md)（配置 provider、审批/沙箱注意事项）。
- **Kimi Code**：国内可直接用、无需海外支付方式，用法和上面两个基本一致。适合 Claude Code / Codex 访问不稳定或没有海外支付的用户。

> 这三个都是"在终端里跟 AI 对话、AI 能读写你电脑上文件"的工具。装哪个都行，本系统不锁定其中任何一个。

---

## Step 2 · 下载本仓库

会用 git 的：

```bash
git clone <this-repo-url> my-literature-library
```

不会 git 也没关系——在 GitHub 页面点 **Code → Download ZIP**，解压到一个你喜欢的位置（比如 `我的文献库`）即可。

---

## Step 3 · 拿一个 API Key

去大模型服务商注册、充值一点点钱、创建一个 API Key（一串 `sk-...` 开头的字符）。

- **默认用 DeepSeek**（<https://platform.deepseek.com>）：最便宜，实测入库 400 篇论文总共约 10 元人民币。
- 也可以换成 **Kimi / 通义千问 / 智谱 GLM / OpenAI / 本地 Ollama** 等任何"OpenAI 兼容"的服务——换法很简单，Step 4 里让 AI 帮你改一行就行。

**先把这串 Key 复制好，Step 4 会用到。** 不用自己研究怎么设置环境变量，交给 AI。

---

## Step 4 · ⭐ 核心：让 AI 帮你把环境配好

用你装好的 Agent CLI **在这个文件夹里**打开一个对话（比如在解压出来的目录里运行 `claude`，或按你所用工具的方式打开），然后**把下面这段整个复制粘贴进去**：

```
请先读一下这个项目里的 AGENTS.md 和 CLAUDE.md，理解这套文献管理系统怎么用、你在里面扮演什么角色。

然后帮我把运行环境配置好，一步步来，每一步做完告诉我结果：
1. 检查我的电脑装没装 Python 3.10 或更高版本；没装就告诉我去哪下载、怎么装。
2. 用 requirements.txt 安装依赖（openai、python-docx、pymupdf、openpyxl、streamlit；如果我在 Windows 且以后想用"Word 里插入引用"功能，再装 pywin32）。
3. 帮我把大模型 API Key 设置成环境变量。我用的是 DeepSeek，Key 是：<在这里粘贴你的 sk- 开头的 Key>。告诉我具体该运行什么命令来设置 DEEPSEEK_API_KEY，Windows 和 Mac/Linux 分别怎么设，设完怎么验证生效。（如果你不方便直接处理我的 Key，就把设置命令写出来让我自己运行。）
4. 建好系统需要的数据目录：inbox、papers、notes、notes-readable、extracted-text、topics、exports、data。
5. 做一次连通性自检：跑一下 scripts/ds.py 的通用对话功能，确认 API Key 能正常调用模型。
6. 全部就绪后，用一句话告诉我现在可以开始"入库"了，并说明我该把 PDF 放到哪个文件夹、然后对你说什么。
```

> **把 `<在这里粘贴你的 sk- 开头的 Key>` 换成你真实的 Key** 再发送。如果你不想让 AI 经手你的 Key，就把那句改成"我自己来设置环境变量，你把 Windows 和 Mac 的设置命令分别写给我"，AI 会把命令给你、你自己运行。
>
> **API Key 是密码性质的东西**：别贴到任何网页、别提交进 git 仓库（本仓库的 `.gitignore` 已经帮你挡掉了 `.env`/`*.key`）。

AI 会照着这个清单一步步做完，中间缺什么（比如没装 Python）它会告诉你怎么补。**做完这一步，环境就配好了。**

> **它大概率还会反过来问你几个问题**——比如你的研究领域是什么、常读哪些顶刊、需要哪些分类标签。这是因为本仓库自带的示例内容是电催化领域的（见下方"把系统改成你自己的领域"），AI 读完 `CLAUDE.md` 会认出这是你第一次用、主动帮你问清楚再去做个性化配置，不用你自己想起来要问它。照实回答就行。

---

## Step 5 · 第一次"入库"

入库 = 把 PDF 变成结构化的精读笔记，存进你的库。

1. 把几篇论文 PDF 拖进 `inbox/` 文件夹。
2. 回到对话，直接说：

```
入库
```

AI 会：读每篇 PDF → 调用便宜的模型抽取标题/作者/DOI/关键词/方法/七节精读笔记 → **自己判断是不是重复收录、DOI 可不可信、补充材料(SI)该配给哪篇** → 写进 `papers/`、`notes/`、`library.bib`、`INDEX.md` 等地方 → 处理完把 `inbox/` 里的原文件清掉。

> **为什么"入库"要走对话、不能纯自动？** 因为查重、核实 DOI、配对补充材料这些需要"结合上下文做判断"的活，正是 Agent CLI 该干的。如果你有一大批不需要人工核实的 PDF，也可以让它跑全自动批处理（说"用 batch_ingest 无人值守批量入库"即可），但日常小批量走对话更稳。

入库完可以直接问它库里的内容，比如"库里有哪些讲质子传导的论文"，它会带着出处 citekey 回答。

---

## Step 6 · 日常就这么用——记住这几句话

配好之后，你几乎只需要对 AI 说这几句大白话（指令名你也可以自己改，AI 读了 CLAUDE.md 就懂）：

| 你说 | AI 会做什么 |
|---|---|
| **入库** | 处理 `inbox/` 里的新 PDF（见 Step 5） |
| **体检** | 检查库里有没有孤儿 PDF、缺笔记、重复收录、格式不一致 |
| **引文献** + 贴一段你正在写的论文正文 | 逐句（含句中逐个列举项）找出能引用的文献，标注每篇是支持/矛盾/信息不足，还告诉你引用编号该插在哪个词后面 |
| **把这几篇导出成 EndNote 能导入的格式** | 生成 RIS/BibTeX，你在 EndNote/Zotero 里一次性批量导入 |
| **在 Word 光标处插入引用**（仅 Windows+已开 Word） | 直接在你打开着的 Word 文档里插入编号引用、维护文末参考文献列表 |
| **扩充 / 查新 某个领域** | 搜该领域顶刊文献、跟库里去重、产出一份待下载清单 |
| **给 XX 主题写个综述** | 筛相关笔记、起草分类对比、生成一份 docx 交付文档 |
| **打开操作面板** | 开一个本地网页界面（浏览库、搜索、体检等），见下 |

### 想要图形界面？

不想全程打字的话，直接对 AI 说"打开操作面板"就行（Step 4 已经帮你装好了这个功能需要的依赖）。这是一个本地网页仪表盘，可以浏览全部文献、搜索、筛选、排序、读笔记、拖 PDF 进 inbox、跑体检/引文献/扩充查新等。

**Windows 用户**：也可以直接**双击文件夹里的 `PaperTrail Launcher.bat`**打开，不用找 AI。

注意：**真正的"入库"仍然要回对话里做**（查重/核实需要判断，网页表单干不了）。

---

## 换掉 DeepSeek，用别的模型

系统调用模型的逻辑集中在 `scripts/ds.py`，走的是标准 OpenAI 接口。想换服务商，直接跟 AI 说：

```
我想把默认的 DeepSeek 换成 <Kimi / 通义 / GLM / OpenAI / 本地 Ollama>，
帮我改 scripts/ds.py 里的 base_url 和读取的环境变量名，改完告诉我新的 Key 环境变量该怎么设。
```

它会帮你改好那一处配置。

---

## 把系统改成你自己的领域（重要）

本仓库自带的示例内容是**电催化/材料科学**领域的（因为它诞生于一个电催化文献库）。这些地方标了 `[领域定制]`，克隆后应换成你自己领域的东西。

**正常情况下你不需要手动做这一步**——Step 4 里 AI 读完 `CLAUDE.md` 会自动识别出这是首次使用，主动问你研究领域、常读期刊、需要的分类标签等问题，问完直接帮你把 `[领域定制]` 的地方全部改掉。

如果它没主动问（比如你是接手别人已经用过的库、或者它判断漏了），自己手动触发也行，把下面这段发给它：

```
我的研究领域是 <你的领域，比如"钙钛矿太阳能电池"/"合成生物学"/"宏观经济学">。
请把项目里所有标了 [领域定制] 的地方（AGENTS.md 里的标签词表、frontmatter 结构化字段、
scripts/top_journals.txt 顶刊名单、batch_ingest.py 里的领域术语和研究背景描述等）
换成适合我这个领域的内容，改完列一份清单告诉我你改了哪些。
```

其余部分（citekey 规则、补充材料绑定、查重、体检、综述工作流）是**领域无关**的，不用改。

---

## 常见问题

- **"AI 说找不到 python / 命令报错"**：把完整报错贴回给 AI，它会告诉你怎么修（大多是没装 Python、没装依赖、或环境变量没设对）。这套系统的哲学就是——出问题直接问 AI，别自己硬啃。
- **"调用模型报 401 / 认证失败"**：API Key 没设对，或者当前这个终端窗口是设 Key 之前打开的。让 AI 帮你确认环境变量，或者重开一个终端窗口再试。
- **"我在 Windows，`bash` 相关的体检命令报奇怪的错"**：Windows 上 `bash` 有时会被系统错误地指到 WSL 而不是 Git Bash。跟 AI 说这个现象，它知道怎么绕开（面板里已经自动处理了）。
- **"入库很慢 / 想知道花了多少钱"**：单篇成本大概几分钱人民币，几百篇也就十几块。详细估算见 [README.md](README.md) 的"处理一篇论文大概花多少钱"。
- **想深入了解数据格式和每个字段的含义**：看 [AGENTS.md](AGENTS.md)（这是整套系统的唯一权威规范）。想了解 AI 作为"架构维护者"的职责边界：看 [CLAUDE.md](CLAUDE.md)。

---

**一句话总结**：装个 AI 助手 → 下载本仓库 → 粘贴 Step 4 那段开场白让 AI 配好环境 → 往 `inbox/` 丢 PDF、说"入库" → 之后用大白话指挥它。就这么简单。
